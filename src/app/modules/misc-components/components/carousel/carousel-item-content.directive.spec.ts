import { CarouselItemContentDirective } from './carousel-item-content.directive';

describe('CarouselItemContentDirective', () => {
  it('should create an instance', () => {
    const directive = new CarouselItemContentDirective();
    expect(directive).toBeTruthy();
  });
});
